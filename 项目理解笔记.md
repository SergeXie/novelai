# NovelAI 项目理解笔记

> 📅 更新时间：2026-07-15
> 📝 用途：快速了解项目架构，为新功能开发提供参考

---

## 一、项目定位

这是一个 **基于 FastAPI + MySQL + 异步AI生成** 的小说创作服务项目，核心特点：
- 采用分层架构（Controller → Service → DAO → AI适配器）
- 以"节点树"方式组织小说内容
- 支持多AI模型适配（豆包、DeepSeek、Claude、GPT、Gemini等）
- 有完整的计费和额度管理系统

---

## 二、核心架构图

```
┌─────────────────────────────────────────────────────────┐
│                    Controller 层                        │
│  ai_controller / book_controller / novel_wizard_...    │
└─────────────────────┬───────────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────────────────┐
│                     Service 层                          │
│  ai_service / book_service / ai_prompt_service / ...   │
└─────────────────────┬───────────────────────────────────┘
                      │
        ┌─────────────┼─────────────┐
        │             │             │
┌───────▼──────┐ ┌───▼────┐ ┌─────▼──────┐
│    DAO 层     │ │  AI层  │ │   核心层   │
│  book_dao    │ │ai_nexus│ │  entity    │
│  ai_log_dao  │ │adapters│ │  enums     │
└──────────────┘ └────────┘ └────────────┘
```

---

## 三、关键数据模型

### 3.1 作品相关

| 表名 | ORM文件 | 说明 |
|------|---------|------|
| `books` | `core/entity/do/books.py` | 作品主表 |
| `mc_book_node` | `core/entity/do/book_node.py` | **核心：节点树** |
| `mc_templates` | `core/entity/do/mc_template.py` | 节点树模板 |
| `mc_global_lexicon` | `core/entity/do/global_lexicon_do.py` | 公共词条库 |
| `ai_novel_generate_log` | `core/entity/do/generate_log.py` | AI生成流水 |

### 3.2 BookNode 节点类型枚举

```python
# core/enums/node_type.py
class BookNodeCategory(Enum):
    NORMAL = (0, "自定义")
    CONTENT = (1, "正文")      # 章节内容
    ROLES = (2, "角色卡")       # 角色设定
    WORLDVIEW = (3, "世界观")   # 世界观设定
    WRITING_STYLE = (4, "写作手法")  # 写作风格
    OUTLINE = (5, "大纲")       # 大纲
```

### 3.3 公共词条库类型

```python
# lexicon_type 字段
1 = 角色
2 = 世界观
3 = 功法
4 = 道具
```

---

## 四、核心业务流程

### 4.1 创建书籍流程

```
create_book_with_tree():
1. 查询模板 (TPLXIAOSHUO)
2. 递归创建 BookNode 节点树
3. 返回书籍ID (bid)
```

### 4.2 AI生成小说流程

```
ai_service.execute():
1. 查询书籍基本信息
2. 渲染提示词模板 (Jinja2)
3. 组装上下文：
   - 书籍基本信息 (书名、简介)
   - 角色卡 (type=2)
   - 世界观 (type=3)
   - 写作手法 (type=4)
   - 前情提要 (历史正文)
4. 关联词条库内容
5. 估算Token并记录日志
6. 调用AI适配器生成
7. 返回生成结果
```

### 4.3 提示词组装逻辑

```python
# ai_prompt_service.py - _assemble_prompt()
拼装顺序：
1. 角色卡列表
2. 世界观
3. 写作手法
4. 自定义节点
5. 前情提要 (历史章节)
```

---

## 五、AI适配器体系

### 5.1 适配器目录

```
ai/adapters/
├── base_adapter.py      # 抽象基类
├── ollama.py            # 本地模型
├── deep_seek.py         # DeepSeek
├── doubao.py            # 豆包 (Ark SDK)
├── doubao_plus.py       # 豆包增强版
├── claude.py            # Claude
├── gemini.py            # Gemini
└── gpt.py               # OpenAI
```

### 5.2 模型配置

- 来源：数据库表 `mc_ai_models`
- 字段：model_name, provider, api_key, base_url, temperature, max_tokens等
- 调用：通过 `ai_nexus.py` 统一调度

---

## 六、角色状态管理现状

### 6.1 当前实现方式

角色通过两种方式存储：

**方式1：节点式角色卡**
- 表：`mc_book_node`
- 类型：`type = 2`
- 存储：`content` 字段存设定文本，`data` JSON 存扩展数据

**方式2：公共词条库**
- 表：`mc_global_lexicon`
- 类型：`lexicon_type = 1`
- 特点：支持跨书籍复用，有分享等级

### 6.2 现有局限

1. **无状态跟踪**：只有静态设定，没有动态状态
2. **无变化历史**：状态更新后覆盖，无法回溯
3. **无冲突检测**：AI生成时不会检查与已有状态的矛盾
4. **无关联关系**：角色间关系需要手动维护

---

## 七、关键文件速查

### 7.1 入口文件
| 文件 | 作用 |
|------|------|
| `main.py` | 程序主入口 |
| `server.py` | FastAPI应用组装 |

### 7.2 核心Service
| 文件 | 作用 |
|------|------|
| `service/ai_service.py` | AI生成核心逻辑 |
| `service/book_service.py` | 书籍CRUD、节点管理 |
| `service/ai_prompt_service.py` | 提示词组装渲染 |
| `service/novel_wizard_controller.py` | 小说向导流程 |

### 7.3 核心DAO
| 文件 | 作用 |
|------|------|
| `dao/book_dao.py` | 书籍数据访问 |
| `dao/ai_log_dao.py` | AI生成日志 |
| `dao/template_dao.py` | 模板数据访问 |

### 7.4 配置文件
| 文件 | 作用 |
|------|------|
| `common/config/config.py` | 统一配置读取 |
| `common/config/get_db.py` | 数据库会话管理 |

---

## 八、数据库关键字段

### 8.1 books 表
```sql
uid        -- 用户ID
bid        -- 书籍ID (唯一)
title      -- 书名
description -- 简介
template_id -- 使用的模板ID
word_count  -- 字数统计
status      -- 状态
```

### 8.2 mc_book_node 表
```sql
bid        -- 书籍ID
uid        -- 用户ID
name       -- 节点名称
type       -- 节点类型 (0-5)
content    -- 核心内容
data       -- JSON扩展数据  ← 可扩展状态跟踪
depth      -- 树深度
is_leaf    -- 是否叶子节点
parent_id  -- 父节点ID
book_len   -- 内容长度
order      -- 排序
```

### 8.3 mc_global_lexicon 表
```sql
user_id      -- 创建者
title        -- 词条名称
lexicon_type -- 类型 (1角色/2世界观/3功法/4道具)
content      -- 详细描述
data         -- JSON扩展
share_level  -- 分享等级 (0私有/1系列/2公开)
```

---

## 九、扩展点与新功能切入点

### 9.1 角色状态跟踪（待实现）

**切入点**：利用 `mc_book_node.data` JSON字段
- 无需新建表
- 在角色卡节点的 data 中添加状态信息
- 修改 `ai_prompt_service.py` 注入状态

### 9.2 角色关系图谱（可扩展）

**切入点**：扩展 `data` JSON 或新建关系表
- 可在节点 data 中添加 `relations` 字段
- 或新建 `character_relations` 表

### 9.3 时间线管理（可扩展）

**切入点**：利用正文节点的排序
- 可在 `data` 中添加时间戳信息
- 或新建 `timeline_events` 表

---

## 十、开发注意事项

### 10.1 代码规范
- 新接口放 `controller/`
- 新业务逻辑放 `service/`
- 数据库操作放 `dao/`
- 新AI模型扩 `ai/adapters/`

### 10.2 数据库Session
- 请求级：通过依赖注入获取
- 后台任务：独立session
- 注意：后台任务不能直接用请求级session

### 10.3 AI调用流程
1. 先记录日志（即使还没生成）
2. 返回 request_id 供轮询
3. 异步执行生成任务
4. 前端通过轮询获取结果

---

## 十一、常用命令

```bash
# 启动项目
python main.py

# 安装依赖
pip install -r requirements.txt

# 数据库迁移脚本位置
scripts/*.sql
```

---

## 十二、待办与想法

> 💡 在这里记录新功能想法，后续开发时可快速定位切入点

- [ ] 角色状态跟踪功能
- [ ] 状态变化历史记录
- [ ] AI生成时的状态冲突检测
- [ ] 角色关系图谱可视化

---

*最后更新：2026-07-15*
