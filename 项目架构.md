# 项目架构

## 1. 项目概述

这是一个基于 **FastAPI + MySQL + 异步 AI 生成工作流** 的小说创作服务项目。整体采用分层架构，将接口、业务、数据访问、AI 模型适配与通用基础设施拆分管理，便于扩展和维护。

## 2. 启动入口

- **`main.py`**：程序主入口，负责初始化日志、创建 FastAPI 应用并启动 Uvicorn。
- **`server.py`**：应用组装入口，负责注册中间件、路由和全局异常处理。

## 3. 分层结构

### 3.1 接口层 `controller/`

负责接收 HTTP 请求、参数校验、依赖注入和返回响应。

主要模块：

- `ai_controller.py`：AI 生成、历史记录、模型列表等接口
- `book_controller.py`：作品相关接口
- `login_controller.py`：登录与认证接口
- `prompt_controller.py`：提示词相关接口
- `template_controller.py`：模板相关接口
- `user_controller.py`：用户相关接口

### 3.2 业务层 `service/`

负责实现核心业务规则和流程编排。

主要模块：

- `ai_service.py`：AI 请求编排、日志记录、后台生成任务
- `usage_service.py`：额度统计、日志写入、生成历史查询
- `user_service.py`：登录、改密、token 创建
- `book_service.py`：作品业务
- `template_service.py`：模板业务
- `ai_prompt_service.py`：提示词拼装与渲染
- `novel_workflow_demo.py`：小说生成工作流示例

### 3.3 数据访问层 `dao/`

负责数据库查询、更新和统计，屏蔽 SQLAlchemy 的细节。

主要模块：

- `base.py`：DAO 基类
- `ai_log_dao.py`：AI 生成日志相关操作
- `ai_model_dao.py`：模型配置查询
- `ai_prompt_registry_dao.py`：提示词注册表操作
- `book_dao.py`：作品数据访问
- `template_dao.py`：模板数据访问
- `user_dao.py`：用户数据访问

### 3.4 AI 适配层 `ai/`

负责统一接入不同 AI 提供商，并对外暴露一致的调用方式。

主要模块：

- `ai_nexus.py`：AI 统一调度中心，按 provider 选择适配器
- `adapters/`：各家模型适配器
  - `ollama.py`
  - `deep_seek.py`
  - `doubao.py`
  - `doubao_plus.py`
  - `claude.py`
  - `gemini.py`
- `adapters/base_adapter.py`：适配器抽象基类
- `adapters/enums.py`：AIProvider 枚举与等级映射
- `config_model.py`：模型配置结构

## 4. 核心支撑模块

### 4.1 配置 `common/config/`

- `config.py`：统一读取环境变量和系统配置
- `get_db.py`：数据库会话管理，提供请求级和任务级 session

### 4.2 异常处理 `common/exception/`

- `handle.py`：FastAPI 全局异常处理注册
- `errors.py`、`lzsd_exception.py`：项目自定义异常

### 4.3 响应封装 `common/response/`

统一封装成功、失败、未授权等响应结构。

### 4.4 日志 `common/log/`

负责应用日志初始化与输出格式配置。

### 4.5 认证依赖 `core/deps/`

- `auth.py`：JWT 认证、用户校验、书籍归属校验
- `token_utils.py`：token 缓存管理

## 5. 数据模型

### 5.1 ORM 实体 `core/entity/do/`

用于映射数据库表，例如：

- `users_do.py`
- `books.py`
- `generate_log.py`
- `mc_ai_model.py`
- `mc_template.py`
- `prompt_register.py`

### 5.2 入参与出参模型 `core/entity/vo/`

用于接口请求和响应数据结构，例如：

- `login_vo.py`
- `user_vo.py`
- `ai_model_vo.py`
- `template_vo.py`
- `prompt_register_vo.py`

### 5.3 通用 Schema `core/entity/schemas.py`

放置接口通用请求体，如生成请求 `GenerateRequest`。

## 6. 一次典型请求的流转

以“生成小说片段”为例：

1. 前端请求 `controller/ai_controller.py` 的 `/generate`
2. 控制器通过依赖注入获取用户、数据库连接和书籍权限
3. 调用 `service/ai_service.py` 做额度检查和日志记录
4. 将耗时生成任务交给后台任务执行
5. 后台任务通过 `ai/ai_nexus.py` 选择具体模型适配器
6. 适配器调用对应 AI 服务生成文本
7. 结果回写到 `dao/ai_log_dao.py` 对应的日志记录中
8. 前端通过轮询接口获取生成结果

## 7. 配置与运行

- 数据库配置由环境变量提供，核心连接串为 `DATABASE_URL`
- 服务端口、CORS、GZip、JWT、额度等均集中在 `common/config/config.py`
- 启动流程通常是运行 `main.py`

## 8. 结构特点

- 接口层薄，业务逻辑主要下沉到 `service/`
- 数据访问统一封装在 `dao/`
- AI 供应商通过适配器模式解耦
- 请求级数据库 session 与后台任务 session 分离
- 全局异常和统一响应封装提升了接口一致性

## 9. 维护建议

- 新增接口优先放到对应 `controller/` 中
- 新增业务规则优先放到 `service/`
- 涉及数据库操作时优先补充 `dao/`
- 新接入模型时只需扩展 `ai/adapters/` 并在 `ai_nexus.py` 注册
